{
  "name": "isobar",
  "type": "module",
  "private": true,
  "workspaces": ["frontend"],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace frontend",
    "lint": "npm run lint --workspace frontend",
    "test": "npm test --workspace frontend",
    "test:fees": "python -m pytest tests/integration --network studio_devnet --fee-profile frontend/fee-profile.json -v -s",
    "deploy": "genlayer deploy"
  },
  "devDependencies": {
    "genlayer-js": "2.0.0-rc.1"
  }
}
